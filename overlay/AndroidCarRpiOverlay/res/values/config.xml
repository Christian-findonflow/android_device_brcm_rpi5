<?xml version="1.0" encoding="utf-8"?>
<!-- NEO dash: the framework default dialer is the phone Dialer
     (com.android.dialer), which this image does not ship, so no app held the
     DIALER role and Telecom's "system dialer" pointed at a package that does
     not exist: the car Dialer's in-call screen was never bound and no active
     call ever showed (found 2026-09-06). Same value as device/generic/car. -->
<resources>
    <string name="config_defaultDialer" translatable="false">com.android.car.dialer</string>
    <!-- AndroidRpiOverlay says the Pi is not voice capable, which makes the
         DIALER role unavailable ("Role is unavailable: android.app.role.DIALER").
         With the phone's calls arriving over the Bluetooth hands-free client
         the dash is voice capable, as AAOS reference devices are. -->
    <bool name="config_voice_capable">true</bool>
    <!-- Let the car SystemUI control the system bars, so an app's immersive
         request (games) hides them as config_systemBarPersistency=1 intends.
         With the framework default (false) the bars never hide and a
         full-screen game draws its top and bottom under the fixed bars. -->
    <bool name="config_remoteInsetsControllerControlsSystemBars">true</bool>
    <!-- And let a swipe from the top or bottom edge bring the hidden bars back
         transiently; without this DisplayPolicy.requestTransientBars ignores
         every edge swipe ("Remote insets controller disallows showing system
         bars") and an immersive game has no way out (no hardware keys). -->
    <bool name="config_remoteInsetsControllerSystemBarsCanBeShownByUserAction">true</bool>
</resources>
